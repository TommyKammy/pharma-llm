# Results Directory

Experiment outputs are local artifacts and are not tracked by default.

Use `results/reports/` for lightweight Markdown indexes and summaries that are safe to track. Large raw outputs, model responses, metrics dumps, and adapter artifacts should remain local or move to a dedicated artifact store.

